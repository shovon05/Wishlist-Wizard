version: '3.8'

services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: wishlist_user
      POSTGRES_PASSWORD: wishlist_password
      POSTGRES_DB: wishlist_wizard
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./setup.sql:/docker-entrypoint-initdb.d/setup.sql

volumes:
  postgres_data:
